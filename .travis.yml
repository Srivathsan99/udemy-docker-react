sudo: required
language: generic

services:
  - docker

before_install:
  - docker build -t srivathsan99/udemy_docker_react -f Dockerfile.dev . 

script:
  - docker run -e CI=true srivathsan99/udemy_docker_react npm run test -- --coverage